# Commits that only reformat code — no behaviour change. `git blame` skips
# these so it keeps pointing at whoever last changed a line's meaning.
#
# Enable locally (once per clone):
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub's blame view honours this file automatically, no setup needed.
#
# Add one full 40-character SHA per line, newest last.

# style: apply Prettier across the repository
3c89611a2d07e811759a81f4fb1ec6cf0c718c14
