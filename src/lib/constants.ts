/** Project metadata shown in the sidebar link and the Settings "About" row. */
export const REPO_URL = 'https://github.com/saddemu/pMusic';
export const APP_VERSION = __APP_VERSION__;

/**
 * The phone breakpoint, in CSS pixels. Kept in sync by hand with the `@media`
 * blocks in `global.css`: CSS decides where the layout changes, this decides
 * what the code does about it (which columns to render, which artwork size to
 * ask the server for).
 */
export const MOBILE_BREAKPOINT = 768;
